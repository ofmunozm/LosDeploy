# Ejemplo de archivo terraform.tfvars
# Copia este archivo a terraform.tfvars y completa con tus valores reales
# IMPORTANTE: terraform.tfvars NO se debe commitear (está en .gitignore)

# AWS Configuration
aws_region   = "us-east-2"
project_name = "blacklist"

# Container Configuration
container_port = 5000

# Database Configuration
db_username = "postgres"
db_password = "TU_PASSWORD_SEGURO_AQUI"  # Cambiar por password real
db_name     = "blacklist_db"

# Application Secrets
secret_key     = "mi_clave_ultra_secreta_123"     # Cambiar por secret real
jwt_secret_key = "mi-jwt-secret-key"              # Cambiar por secret real
static_token   = "token-estatico-123456"          # Cambiar por token real
